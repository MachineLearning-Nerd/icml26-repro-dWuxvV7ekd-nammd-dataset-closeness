# Evaluation

Pre-run status: `BLOCKED`. The contract is eligible for `VERIFIED` only after
the committed fixed command succeeds and its ORX log records the raw range,
independent result, negative-control result, Git SHA, environment, and runtime.
No empirical Gaussian trend is accepted as a substitute for the definition.
