# Evaluation

Pre-run verdict: `BLOCKED`. A `FALSIFIED` result requires rejection probability
strictly above 0.05, independent exact agreement, and a successful mutation
control. Otherwise the finite statement remains `BLOCKED`; no Monte Carlo
tolerance or permutation proxy is used.
